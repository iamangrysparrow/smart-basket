<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================= -->
    <!-- HandyControl Color Overrides                 -->
    <!-- These MUST come AFTER HandyControl themes    -->
    <!-- to override their default orange accent      -->
    <!-- ============================================= -->

    <!-- Re-override PrimaryColor that HandyControl's SkinDefault overwrites -->
    <!-- Note: We use DynamicResource to our AccentDefault color so theme switching works -->
    <!-- The AccentDefault color is defined in Colors.Light.xaml / Colors.Dark.xaml -->

    <!-- HandyControl Brush overrides - use our accent colors -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="{DynamicResource AccentDefault}"/>
    <SolidColorBrush x:Key="DarkPrimaryBrush" Color="{DynamicResource AccentPressed}"/>
    <SolidColorBrush x:Key="LightPrimaryBrush" Color="{DynamicResource AccentSubtle}"/>

    <!-- CRITICAL: Override AccentBrush that HandyControl overwrites with orange -->
    <SolidColorBrush x:Key="AccentBrush" Color="{DynamicResource AccentDefault}"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="{DynamicResource AccentHover}"/>
    <SolidColorBrush x:Key="AccentPressedBrush" Color="{DynamicResource AccentPressed}"/>
    <SolidColorBrush x:Key="AccentSubtleBrush" Color="{DynamicResource AccentSubtle}"/>
    <SolidColorBrush x:Key="AccentMutedBrush" Color="{DynamicResource AccentMuted}"/>

    <!-- Additional HC keys that might use accent -->
    <SolidColorBrush x:Key="PrimaryTextBrush" Color="{DynamicResource AccentDefault}"/>
    <SolidColorBrush x:Key="SecondaryBorderBrush" Color="{DynamicResource BorderDefault}"/>

    <!-- Semantic colors -->
    <SolidColorBrush x:Key="DangerBrush" Color="{DynamicResource Error}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{DynamicResource Warning}"/>
    <SolidColorBrush x:Key="InfoBrush" Color="{DynamicResource Info}"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="{DynamicResource Success}"/>

    <!-- Modern GroupBox style with proper theming -->
    <Style x:Key="ModernGroupBox" TargetType="GroupBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="Margin" Value="0,0,0,12"/>
        <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GroupBox">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Border Grid.Row="0"
                                Background="{DynamicResource AccentSubtleBrush}"
                                BorderBrush="{DynamicResource AccentBrush}"
                                BorderThickness="1,1,1,0"
                                CornerRadius="4,4,0,0"
                                Padding="12,8">
                            <ContentPresenter ContentSource="Header"
                                              TextElement.Foreground="{DynamicResource ForegroundPrimaryBrush}"
                                              TextElement.FontWeight="SemiBold"
                                              TextElement.FontSize="12"/>
                        </Border>

                        <!-- Content -->
                        <Border Grid.Row="1"
                                Background="{DynamicResource BackgroundBaseBrush}"
                                BorderBrush="{DynamicResource AccentBrush}"
                                BorderThickness="1,0,1,1"
                                CornerRadius="0,0,4,4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter TextElement.Foreground="{DynamicResource ForegroundPrimaryBrush}"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Settings ListBox style with proper theming -->
    <Style x:Key="SettingsListBoxItem" TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimaryBrush}"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource BorderDefaultBrush}"
                            BorderThickness="0,0,0,1"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{DynamicResource BackgroundLayer2Brush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd" Property="Background"
                                    Value="{DynamicResource AccentSubtleBrush}"/>
                            <Setter TargetName="Bd" Property="BorderBrush"
                                    Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SettingsListBox" TargetType="ListBox">
        <Setter Property="Background" Value="{DynamicResource BackgroundBaseBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderDefaultBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource SettingsListBoxItem}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <ScrollViewer Padding="{TemplateBinding Padding}">
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
