<Window x:Class="SmartBasket.WPF.Views.ProductDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Создать продукт"
        Width="350" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Name -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Название:" Style="{StaticResource TextPrimary}" Margin="0,0,0,4"/>
            <TextBox x:Name="NameTextBox" Style="{StaticResource TextInput}"
                     MaxLength="255"/>
        </StackPanel>

        <!-- Category -->
        <StackPanel Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Text="Категория:" Style="{StaticResource TextPrimary}" Margin="0,0,0,4"/>
            <ComboBox x:Name="CategoryComboBox" FontSize="11">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding DisplayName}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Style="{StaticResource SecondaryButton}"
                    IsCancel="True" Width="80" Margin="0,0,8,0"/>
            <Button Content="Сохранить" Style="{StaticResource PrimaryButton}"
                    Click="SaveButton_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
