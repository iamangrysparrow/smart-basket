<Window x:Class="SmartBasket.WPF.Views.LabelDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Создать метку"
        Width="320" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Name -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Название:" Style="{StaticResource TextPrimary}" Margin="0,0,0,4"/>
            <TextBox x:Name="NameTextBox" Style="{StaticResource TextInput}"
                     MaxLength="255"/>
        </StackPanel>

        <!-- Color Picker -->
        <StackPanel Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Text="Цвет:" Style="{StaticResource TextPrimary}" Margin="0,0,0,4"/>
            <WrapPanel x:Name="ColorPalette">
                <!-- Colors will be added in code-behind -->
            </WrapPanel>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Style="{StaticResource SecondaryButton}"
                    IsCancel="True" Width="80" Margin="0,0,8,0"/>
            <Button Content="Сохранить" Style="{StaticResource PrimaryButton}"
                    Click="SaveButton_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
