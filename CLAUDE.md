# Smart Basket

> Приложение для автоматического сбора и анализа чеков из различных источников с использованием AI для категоризации.

## Быстрый старт

```bash
cd src
dotnet build
dotnet run --project SmartBasket.WPF
```

---

## Архитектура проекта

```
src/
├── SmartBasket.Core/        # Сущности, конфигурация (без зависимостей)
├── SmartBasket.Data/        # EF Core DbContext, PostgreSQL/SQLite
├── SmartBasket.Services/    # Бизнес-логика, AI провайдеры, парсеры
├── SmartBasket.WPF/         # UI приложение (MVVM)
└── SmartBasket.CLI/         # Консольные утилиты для тестирования
```

### Ключевые сущности

| Сущность | Описание |
|----------|----------|
| **Product** | Категория товара (иерархия через ParentId) |
| **Item** | Конкретный товар из чека |
| **Receipt** | Чек (магазин, дата, позиции) |
| **ReceiptItem** | Позиция в чеке (количество, цена) |
| **Label** | Пользовательская метка |

### Подробная документация

- `ARCHITECTURE.md` — полная архитектура системы
- `ARCHITECTURE-AI.md` — интеграция с AI (⚠️ УСТАРЕЛА, смотри код в SmartBasket.Services/AI/)
- `WPF_RULES.md` — **ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ** перед работой с UI

---

## Дизайн-система

### Статус внедрения

**Текущий шаг: 12 (Шаг 11 завершён - Content полностью стилизован)**

> После завершения каждого шага обнови номер здесь.

### Файлы дизайн-системы

```
UI-UX-Design/
├── SmartBasket-DesignGuide.md        # Полный гайд: цвета, типографика, компоненты
├── SmartBasket-Implementation-Plan.md # Пошаговый план внедрения (14 шагов)
├── SmartBasket-Mockup.html           # Визуальный референс: экран "Чеки"
└── SmartBasket-Products-Mockup.html  # Визуальный референс: экран "Продукты"
```

### Существующие темы (интегрироваться с ними!)

```
SmartBasket.WPF/Themes/
├── ThemeManager.cs      # Уже есть! Использовать его
├── LightTheme.xaml      # Заменить цвета на новые
├── DarkTheme.xaml       # Заменить цвета на новые
├── SharedStyles.xaml    # Расширить новыми стилями
└── Icons.xaml           # Оставить как есть
```

---

## Правила разработки

### 🔴 КРИТИЧЕСКИ ВАЖНО: Не делать заглушки!

**ЗАПРЕЩЕНО:**
```csharp
// TODO: implement later
private void DoSomething() { }

// Stub
public async Task ProcessAsync() => Task.CompletedTask;

// Temporary removed
// <Button Command="{Binding SaveCommand}" />
```

**ОБЯЗАТЕЛЬНО:**
- При рефакторинге **ПОЛНОСТЬЮ ПЕРЕПИСЫВАТЬ** существующий код
- Если функционал временно не нужен — **удалить с комментарием что удалено**
- Если не уверен как реализовать — **спросить, а не делать заглушку**
- Каждый коммит должен оставлять приложение **полностью рабочим**

### Правило переноса функционала

При изменении UI компонента:
1. Сначала изучи текущую реализацию (биндинги, команды, события)
2. Перенеси ВСЮ логику в новую реализацию
3. Проверь что все команды подключены
4. Убедись что приложение компилируется и работает
5. Только потом коммить

### WPF специфика

**ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ:** `WPF_RULES.md`

Краткие правила:
- Используй `DynamicResource` для всех цветов и кистей (для смены тем)
- Не используй `StaticResource` для цветов
- Все стили компонентов выносить в `Themes/`
- MVVM: логика в ViewModel, не в code-behind
- CommunityToolkit.Mvvm для команд и свойств

### Структура стилей

```xml
<!-- ПРАВИЛЬНО: DynamicResource для цветов -->
<Setter Property="Background" Value="{DynamicResource BackgroundLayer1Brush}"/>

<!-- НЕПРАВИЛЬНО: хардкод цветов -->
<Setter Property="Background" Value="#F8F8F8"/>

<!-- НЕПРАВИЛЬНО: StaticResource для цветов -->
<Setter Property="Background" Value="{StaticResource BackgroundLayer1Brush}"/>
```

---

## Команды

### Сборка

**ВАЖНО:** Всегда использовать полные пути от корня репозитория!

```bash
# Корень репозитория
D:\AI\smart-basket

# Сборка всего решения (ВСЕГДА использовать этот путь!)
dotnet build D:\AI\smart-basket\src\SmartBasket.sln

# Или если нужно собрать конкретный проект
dotnet build D:\AI\smart-basket\src\SmartBasket.WPF\SmartBasket.WPF.csproj
```

**НЕ ДЕЛАТЬ:**
```bash
# ❌ Не использовать относительные пути
cd src && dotnet build
dotnet build .
dotnet build SmartBasket.sln
```

### Миграции БД

```bash
# Создать миграцию
dotnet ef migrations add MigrationName --project D:\AI\smart-basket\src\SmartBasket.Data\SmartBasket.Data.csproj --startup-project D:\AI\smart-basket\src\SmartBasket.WPF\SmartBasket.WPF.csproj

# Применить миграции
dotnet ef database update --project D:\AI\smart-basket\src\SmartBasket.Data\SmartBasket.Data.csproj --startup-project D:\AI\smart-basket\src\SmartBasket.WPF\SmartBasket.WPF.csproj
```

### Запуск приложения

**Приложение запускает пользователь вручную.** Claude Code не запускает WPF приложение.

---

## Workflow для UI задач

### При работе с дизайн-системой

1. **Прочитай план:** `UI-UX-Design/SmartBasket-Implementation-Plan.md`
2. **Найди текущий шаг** в этом файле (секция "Статус внедрения")
3. **Выполни шаг** согласно критериям готовности
4. **Проверь сборку:** `dotnet build D:\AI\smart-basket\src\SmartBasket.sln`
5. **Обнови номер шага** в этом файле
6. **Коммит**

### При изменении существующего View

1. Открой `.xaml` и `.xaml.cs` файлы
2. Изучи все биндинги и команды
3. Найди соответствующий ViewModel
4. Внеси изменения, сохраняя ВСЕ биндинги
5. Проверь работоспособность
6. Коммит

---

## Структура Views

```
SmartBasket.WPF/Views/
├── HistoryView.xaml              # Главный экран: список чеков + детали
├── ProductsItemsView.xaml        # Экран: продукты и товары
├── CategoriesView.xaml           # Экран: управление категориями
├── SettingsView.xaml             # Общие настройки (устаревший?)
├── Settings/
│   ├── SettingsWindow.xaml       # Окно настроек
│   ├── SourcesSettingsView.xaml  # Настройки источников
│   ├── ParsersSettingsView.xaml  # Настройки парсеров
│   ├── AiProvidersSettingsView.xaml  # Настройки AI провайдеров
│   └── AiOperationsSettingsView.xaml # Настройки AI операций
└── Dialogs/
    ├── ItemCardDialog.xaml       # Карточка товара
    ├── ProductDialog.xaml        # Редактирование продукта
    ├── LabelDialog.xaml          # Редактирование метки
    ├── CategoriesDialog.xaml     # Выбор категории
    └── ChangeCategoryDialog.xaml # Смена категории
```

---

## Чеклист перед коммитом

- [ ] Код компилируется без ошибок (`dotnet build D:\AI\smart-basket\src\SmartBasket.sln`)
- [ ] Нет закомментированного рабочего кода
- [ ] Нет TODO/FIXME заглушек
- [ ] Все биндинги на месте (не удалены при рефакторинге)
- [ ] Использованы DynamicResource для цветов (если менял UI)

---

## FAQ

### Где искать цвета для новых компонентов?
`UI-UX-Design/SmartBasket-DesignGuide.md` → секция "Цветовая система"

### Как добавить новый стиль?
1. Создай стиль в `Themes/SharedStyles.xaml` или отдельном файле
2. Используй `DynamicResource` для цветов
3. Подключи в `App.xaml` если отдельный файл

### Как переключать тему?
Использовать существующий `ThemeManager.cs` в `SmartBasket.WPF/Themes/`

### Что делать если не понимаю как реализовать?
1. Спроси перед тем как делать заглушку
2. Покажи текущий код и опиши задачу
3. Получи решение и реализуй полностью

---

## Стек технологий

- **.NET 8**, WPF, EF Core
- **БД:** PostgreSQL / SQLite
- **AI:** Ollama (локально), YandexGPT (API)
- **UI:** CommunityToolkit.Mvvm, HandyControl (для сложных контролов)

---

## UI библиотеки

### Гибридный подход

| Что | Откуда |
|-----|--------|
| Кнопки, TextBox, ListBox, панели, метки | Свои стили (Themes/) |
| DatePicker, TimePicker, NumericUpDown | HandyControl |
| TreeView, DataGrid | Свои стили (если нужно кастомизировать) или HandyControl |

### HandyControl

```bash
# Установка
dotnet add D:\AI\smart-basket\src\SmartBasket.WPF\SmartBasket.WPF.csproj package HandyControl
```

**Подключение в App.xaml:**
```xml
<Application.Resources>
    <ResourceDictionary>
        <ResourceDictionary.MergedDictionaries>
            <!-- Наши темы (ПЕРВЫМИ!) -->
            <ResourceDictionary Source="Themes/Colors.Light.xaml"/>
            <ResourceDictionary Source="Themes/Brushes.xaml"/>
            <ResourceDictionary Source="Themes/Sizes.xaml"/>
            
            <!-- HandyControl (после наших тем) -->
            <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
            <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            
            <!-- Наши стили контролов (последними — перезаписывают) -->
            <ResourceDictionary Source="Themes/Controls/ButtonStyles.xaml"/>
        </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
</Application.Resources>
```

**Использование:**
```xml
<!-- Namespace -->
xmlns:hc="https://handyorg.github.io/handycontrol"

<!-- DatePicker -->
<hc:DatePicker SelectedDate="{Binding StartDate}" 
               Style="{StaticResource DatePickerExtend}"/>

<!-- NumericUpDown -->
<hc:NumericUpDown Value="{Binding Count}" 
                  Minimum="1" Maximum="100"/>
```

**Кастомизация цветов HandyControl:**
```xml
<!-- В Colors.Light.xaml добавить -->
<Color x:Key="PrimaryColor">#7C4DFF</Color>
<SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
```

HandyControl подхватывает `PrimaryBrush` для акцентных элементов.

---

*Последнее обновление: 12.12.2025*
